name: My First CI Pipeline 
on:[push] #Run this every time i push code 

jobs:
build-and-test:
runs-on: unbuntu-latest #use a linux server hosted by Github

steps: 
- name: Checkout code 
  uses: actions/checkout@v3

- name: Set up Python 
  uses: actions/setup-python@v4
  with: 
   python-version: '3.9'

- name: Run Tests 
  run: python test_app.py 